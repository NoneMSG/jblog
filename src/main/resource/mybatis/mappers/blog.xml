<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="blog">

	<insert id="createBlog" parameterType="map">
		<![CDATA[
			insert
				into blog
			values (#{userNo}, #{title}, #{logoPath} )
		]]>
	</insert>
	
	<select id="viewBlogMain" parameterType="long" resultType="blogvo">
	<![CDATA[
			select title, log as logopath 
			from blog 
			where user_no=#{userNo}
		]]>
	</select>
	
	

	<select id="viewCategory" parameterType="long" resultType="blogvo">
	<![CDATA[
			select c.name, c.no 
			from blog b, category c 
			where b.user_no=c.user_no
		]]>
	</select>

	<select id="viewPost" parameterType="long" resultType="blogvo">
	<![CDATA[
			select p.title, date_format(p.reg_date, '%Y-%m-%d %p %h:%i:%s') as regDate
			from post p, category c 
			where c.no=p.category_no
			order by regDate desc
		]]>
	</select>


</mapper>
